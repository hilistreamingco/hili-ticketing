import { Link } from "react-router-dom";
import { ArrowLeft, Bell, CalendarDays, MapPin } from "lucide-react";
import Layout from "@/components/layout/Layout";
import { Button } from "@/components/ui/button";
import { events, formatEventDate } from "@/lib/events";

export default function FuturePage() { const event = events[1]; return <Layout><div className="container max-w-5xl py-10 md:py-16"><Link to="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="h-4 w-4" /> Back to home</Link><div className="mt-8 grid gap-10 md:grid-cols-[.8fr_1.2fr] md:items-center"><img src={event.coverImage} alt={`${event.title} preview`} className="aspect-[4/5] w-full rounded-[2rem] object-cover" /><div><span className="inline-flex items-center gap-2 rounded-full bg-[#e7edd0] px-3 py-1.5 text-xs font-bold text-[#536100]"><Bell className="h-3.5 w-3.5" /> Coming soon</span><h1 className="mt-6 font-display text-5xl font-bold leading-[.95] tracking-tight sm:text-7xl">{event.title}</h1><p className="mt-6 max-w-lg text-lg leading-8 text-muted-foreground">{event.shortDescription}</p><div className="mt-8 grid gap-5 border-y border-border py-6 sm:grid-cols-2"><div className="flex gap-3"><CalendarDays className="h-5 w-5 text-[#657600]" /><div><p className="text-xs text-muted-foreground">Proposed date</p><p className="mt-1 font-semibold">{formatEventDate(event.date)}</p></div></div><div className="flex gap-3"><MapPin className="h-5 w-5 text-[#657600]" /><div><p className="text-xs text-muted-foreground">Location</p><p className="mt-1 font-semibold">{event.venue}, {event.city}</p></div></div></div><p className="mt-7 text-sm leading-7 text-muted-foreground">We are putting this one together now. Tickets and full event details will appear here when the event is ready to announce.</p><Button asChild variant="secondary" className="mt-8"><a href={`mailto:hilistreaming.co@gmail.com?subject=Notify me about ${encodeURIComponent(event.title)}`}>Notify me when it opens</a></Button></div></div></div></Layout>; }
