import { Link } from "react-router-dom";
import { ArrowDownRight, ArrowRight, CalendarDays, MapPin, Ticket } from "lucide-react";
import Layout from "@/components/layout/Layout";
import { Button } from "@/components/ui/button";
import { events, formatEventDate, formatEventTime, startingPrice } from "@/lib/events";

const hiliMark = "https://cdn.builder.io/api/v1/image/assets%2F4f039897198345dd9424e863acd695f%2Fe44fa8e2e89c47159514babc8d5107ff?format=webp&width=800&height=1200";
const upcoming = events[0];

export default function Index() {
  return <Layout><section className="bg-[#c1ff1a] text-[#0b0b0b]"><div className="container grid min-h-[calc(100vh-4rem)] items-center gap-12 py-16 lg:grid-cols-[.9fr_1.1fr] lg:py-20"><div className="max-w-xl"><p className="text-[10px] font-semibold uppercase tracking-[.3em]">01 / Upcoming event</p><h1 className="mt-7 font-display text-5xl font-bold leading-[.95] tracking-[-.07em] sm:text-7xl md:text-8xl">Good times<br /><span className="font-serif font-normal italic">start here.</span></h1><p className="mt-8 max-w-sm text-base leading-7 text-black/65">One platform for the events that bring us together. Discover the next Hili experience and come as you are.</p><Button asChild size="lg" variant="secondary" className="mt-8"><Link to={`/events/${upcoming.slug}`}>Get tickets <ArrowRight className="h-4 w-4" /></Link></Button><div className="mt-16 flex h-20 w-20 items-center justify-center rounded-full border border-black/40"><ArrowDownRight className="h-8 w-8" /></div></div><div className="relative mx-auto w-full max-w-[510px]"><div className="absolute -left-4 -top-4 z-10 flex h-14 w-14 items-center justify-center rounded-full bg-black text-sm font-bold text-[#c1ff1a]">01</div><div className="overflow-hidden rounded-[2rem] bg-black shadow-2xl"><img src={hiliMark} alt="Hili Summer Fest" className="aspect-[4/5] w-full object-cover object-center opacity-90" /></div><div className="absolute -bottom-5 -right-4 rounded-2xl bg-white p-4 shadow-xl sm:-right-7"><p className="text-[10px] font-semibold uppercase tracking-widest text-black/45">Next up</p><p className="mt-1 font-display text-sm font-bold">{formatEventDate(upcoming.date)}</p></div></div></div></section><section className="bg-background"><div className="container grid gap-12 py-20 md:grid-cols-[1fr_1.2fr] md:py-28"><div><p className="text-[10px] font-semibold uppercase tracking-[.3em] text-muted-foreground">02 / The next one</p><div className="mt-7 flex items-start gap-4"><span className="mt-1 flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-primary"><Ticket className="h-5 w-5" /></span><div><h2 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">{upcoming.title}</h2><p className="mt-3 max-w-md text-muted-foreground">{upcoming.shortDescription}</p></div></div></div><div><div className="grid gap-5 border-y border-foreground/15 py-6 sm:grid-cols-3"><Detail icon={CalendarDays} label="When" value={`${formatEventDate(upcoming.date)} · ${formatEventTime(upcoming.startTime)}`} /><Detail icon={MapPin} label="Where" value={`${upcoming.venue}, ${upcoming.city}`} /><Detail icon={Ticket} label="Tickets from" value={startingPrice(upcoming)} /></div><p className="mt-7 max-w-lg text-sm leading-7 text-muted-foreground">{upcoming.description}</p><Link to={`/events/${upcoming.slug}`} className="mt-7 inline-flex items-center gap-2 text-sm font-bold underline decoration-primary decoration-2 underline-offset-4">See event details <ArrowRight className="h-4 w-4" /></Link></div></div></section><section className="bg-[#0b0b0b] text-white"><div className="container flex flex-col justify-between gap-8 py-16 md:flex-row md:items-end md:py-20"><div><p className="text-[10px] font-semibold uppercase tracking-[.3em] text-white/45">Hili events</p><h2 className="mt-4 max-w-2xl font-display text-4xl font-bold leading-tight tracking-tight sm:text-5xl">Made for people who want to be there.</h2></div><Button asChild variant="outline" className="w-fit border-white/25 bg-transparent text-white hover:bg-white hover:text-black"><Link to={`/events/${upcoming.slug}`}>View the event <ArrowRight className="h-4 w-4" /></Link></Button></div></section></Layout>;
}
function Detail({ icon: Icon, label, value }: { icon: typeof CalendarDays; label: string; value: string }) { return <div className="flex gap-3"><Icon className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><div><p className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div></div>; }
